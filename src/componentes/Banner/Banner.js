import './Banner.css'

function Banner(){
    //JSX é isso, e é como o react trabalha com a parte visual
    return(
        <header className="banner">
            <img src="/imagens/banner.png" alt="Banner principal da página do Organo"></img>
        </header>
        )
}

export default Banner;